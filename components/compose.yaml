services:
  zig:
    build:
      context: ./${ZIG_ID}
      args:
        - OS_IMG=${OS_IMG}
        - ZIG_TAG=${ZIG_TAG}
        - HTTP_PROXY=${http_proxy:-${HTTP_PROXY:-}}
        - HTTPS_PROXY=${https_proxy:-${HTTPS_PROXY:-}}
    image: ${ZIG_IMG}
