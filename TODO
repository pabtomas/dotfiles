- .vimrc:
  - unsource .vimrc (for big files)
  - toggle set paste
  - toggle hightlight search
  - NERDtree and +clientserver feature: mapping to open buffers into another vim
  - vim9script
- .tmux.conf:
  - add keybinding to change color theme
- update env.d/10tag with bot
- Add a cache volume (1 for all or 1 for each ?) for SSHD containers (find a way to sync it: git/docker-secret/backup ?):
  - bash_history
  - vim sessions
  - tmux resurrect files
  - tmuxinator conf sessions
- logger alias for controller
- decorator:
  - figlet/lolcat/tui monitoring/socat
  - motd
  - dedicated service network
- wget_me.sh:
  - pull latest workspace images from registry
  - make the script works without internet (use cache)
- add aliases/remove old directory
- use dockle/hadolint into CI:
  - readonly: true for all containers
  - use unprivileged user for bg containers (listener, proxy, xserver, relay, ...)
- find a solution to put SHELL templates outside of Dockerfiles
- jumper, listener, proxy, xserver -> change into a layer or model
- split services/volumes/networks ???
- solve docker run 409
- runners:
  - relay + GUI:
    - window manager/Xephyr conf
  - firefox:
    - dedicated service network
  - white-board:
    - tldraw/excalidraw/draw.io
    - on same firefox network
  - secretary:
    - hedgedoc
    - on same firefox network
  - pro
- healthcheck for registry: docker push
