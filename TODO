'-'   -> go for it
'?'   -> go for it but adjust the need to the V2 proposition
'??'  -> go for it but not now

TODO:
- .vimrc: unsource .vimrc
- .vimrc: toggle set paste
- .vimrc: toggle hightlight search
- .vimrc + NERDtree: mapping to open/close tree pane
?? .vimrc: vim9script
?? .tmux.conf: add keybinding to change color theme
- Add a cache volume to gather for container (find a way to sync it: git/docker secret/ backup ?):
  - bash_history
  - vim sessions
  - tmux resurrect files
  - tmuxinator conf sessions
- find a solution to split templates and Dockerfiles
- jumper/proxy -> change them into components
- add permissions for controller
- logger alias for controller
- add aliases/remove old directory
- process:
  O wget the wget_me.sh script
  - run the wget_me.sh script:
    - if Docker not installed:
      O install Docker
      - min (or another modern minimal web browser)
    - else:
      O update Docker
      - min (or another modern minimal web browser)
    O add host docker config
    O pull docker-git image
    O run docker-git container
    O pull workspace git repo
    - pull latest workspace images from registry
    O docker compose create + start into workspace git repo
    O remove workspace git repo
    O docker attach jumper (no ssh)
    O stop + rm all containers
    O rm all containers
    O docker prune everything EXCEPT VOLUMES
- new compose.yaml file:
  - 1 dir = 1 ALPINE Dockerfile = 1 service:
    - decorator:
      - figlet/lolcat/tui monitoring/xinetd/socat + service network
      - motd
    - drafter:
      - tldraw/excalidraw/draw.io
    - notetaker:
      - hedgedoc
    - jlesage/docker-firefox
    - pro: aliases/packages for pro usage
- jlesage/docker-baseimage-gui based images working with min
- update Dockerfiles + images with bot

proposition:
  workspaces
  ├── compose.yaml/templated
  ├── wget_me.sh
  ├── .github/workflows/bot.yml
  ├── host
  ├── proxy/jumper/collector
  │   └── Dockerfile/templated
  ├── components
  │   ├── compose.yaml/templated
  │   └── bash/sshd/docker/linguist/pass/git/vim/tmux/zig
  │       └── Dockerfile/templated
  ├── explorers
  │   ├── compose.yaml/templated
  │   └── zig/shell
  │       └── Dockerfile/templated
  └── runners
      └── spaceporn
          └── Dockerfile/compose.yaml/templates
