---

id: '{{ (datasource "datasources/entrypoint.yaml").id }}{{ (datasource "datasources/all.yaml").sep.id }}{{ (datasource "datasources/bash.yaml").id }}'
image: '{{ (datasource "datasources/registry.yaml").prefix.layer }}{{ (datasource "datasources/entrypoint/bash.yaml").id }}'

...
