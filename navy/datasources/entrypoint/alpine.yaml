---

id: '{{ (datasource "datasources/entrypoint.yaml").id }}{{ (datasource "datasources/all.yaml").sep.id }}{{ (datasource "datasources/alpine.yaml").id }}'
image: '{{ (datasource "datasources/registry.yaml").prefix.layer }}{{ (datasource "datasources/entrypoint/alpine.yaml").id }}'

...
