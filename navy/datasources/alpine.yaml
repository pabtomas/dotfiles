---

id: 'alpine'
image:
  source: 'docker.io{{ (datasource "datasources/all.yaml").sep.registry }}library{{ (datasource "datasources/all.yaml").sep.registry }}{{ (datasource "datasources/alpine.yaml").id }}'
  local: '{{ (datasource "datasources/registry.yaml").prefix.local }}{{ (datasource "datasources/alpine.yaml").id }}'
tag: '3.20'

...
