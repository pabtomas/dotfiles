---

id: 'nginx'
path:
  etc:
    nginx: '/etc/nginx'
    confd: '{{ (datasource "datasources/nginx.yaml").path.etc.nginx }}/conf.d'
    httpd: '{{ (datasource "datasources/nginx.yaml").path.etc.nginx }}/http.d'

  var:
    log:
      nginx: '/var/log/nginx'

...
