---

id: '{{ (datasource "datasources/spaceporn.yaml").id }}{{ (datasource "separators.yaml").id }}{{ (datasource "datasources/runner.yaml").id }}'
host: '{{ (datasource "datasources/spaceporn/runner.yaml").id }}'
image:
tag:
ip:

...
