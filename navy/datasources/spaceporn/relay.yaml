---

id: '{{ (datasource "datasources/spaceporn.yaml").id }}{{ (datasource "separators.yaml").id }}{{ (datasource "datasources/relay.yaml").id }}'
host: '{{ (datasource "datasources/spaceporn/relay.yaml").id }}'
image:
tag:
ip:

...
