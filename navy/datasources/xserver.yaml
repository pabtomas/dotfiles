---

id: 'xserver'
host: '{{ (datasource "datasources/xserver.yaml").id }}'
image: '{{ (datasource "datasources/registry.yaml").prefix.owner }}{{ (datasource "datasources/xserver.yaml").id }}'
ip: '{{ (datasource "datasources/networks.yaml").proxified_xserver_socket.prefix }}.2'
path:
  sockets: '/tmp/.X11-unix'
  display: '{{ (datasource "datasources/xephyr.yaml").path.sockets }}/X{{ .Env.XEPHYR_DISPLAY }}'
port: 2365

...
