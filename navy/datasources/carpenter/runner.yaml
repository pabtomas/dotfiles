---

id: '{{ (datasource "datasources/carpenter.yaml").id }}{{ (datasource "separators.yaml").id }}{{ (datasource "datasources/runner.yaml").id }}'
host: '{{ (datasource "datasources/carpenter/runner.yaml").id }}'
image:
tag:
ip:

...
