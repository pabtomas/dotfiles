---

id: '{{ (datasource "datasources/carpenter.yaml").id }}{{ (datasource "datasources/all.yaml").sep.id }}{{ (datasource "datasources/relay.yaml").id }}'
host: '{{ (datasource "datasources/carpenter/relay.yaml").id }}'
image: '{{ (datasource "datasources/registry.yaml").prefix.relay }}{{ (datasource "datasources/carpenter.yaml").id }}'
ip: '{{ (datasource "datasources/networks.yaml").proxified_xserver_socket.prefix }}.3'

...
