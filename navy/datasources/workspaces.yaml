---

id: 'workspaces'
image: '{{ $registry.prefix.layer }}{{ $workspaces.id }}'
path:
  root: '/{{ $workspaces.id }}'
  spaceporn: '{{ $workspaces.root }}/{{ $spaceporn.id }}'
  project: '{{ $workspaces.root }}/{{ $project.id }}'

...
