---

user:
  unprivileged: 'visitor'

test:
  bad: "{{ $all.test.bad2 }}"
  bad2: "{{ $all.test.bad }}"

os:
  id: '{{ $alpine.id }}'
  image:
    local: '{{ $alpine.image.local }}'
  tag: '{{ $alpine.tag }}'

sep:
  id: '.'
  host: '.'
  registry: '/'
  image: '.'
  tag: ':'
  target: ':'

owner:
  name: 'tiawl'
  tag: 'latest'
  url: 'https://github.com/{{ $all.owner.name }}'

path:
  bin: '/usr/local/bin'
  opt:
    data: '/opt/data'
    scripts: '/opt/scripts'
    ssh: '/opt/ssh'
  root:
    ssh: '/root/.ssh'
    passwd: '/root/.password-store'
  var:
    log: '/var/log'
  cwd: '{{ .Env.CWD }}'
  dockerfiles: '{{ .Env.CWD }}/dockerfiles'

...
