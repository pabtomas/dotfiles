doc:
- datasources -> inventory
- YAML --> JSON
- Golang --> Mustache
- objects:
  - processor
  - command
- reread it totally
- translate exemples into JSON

mwf:
- YAML --> JSON
- remove $ prefix into templates

zig:
- libjq.zig
