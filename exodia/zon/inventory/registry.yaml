---

id: 'registry'
host: '{{ $registry.id }}'
ip: '{{ $networks.docker_utils.prefix }}.3'
prefix:
  global: '{{ $registry.target }}{{ $all.sep.registry }}{{ $project.id }}{{ $all.sep.registry }}'
  owner: '{{ $registry.prefix.global }}{{ $all.owner.name }}{{ $all.sep.image }}'
  local: '{{ $registry.prefix.owner }}local{{ $all.sep.image }}'
  layer: '{{ $registry.prefix.owner }}layer{{ $all.sep.image }}'
  relay: '{{ $registry.prefix.owner }}relay{{ $all.sep.image }}'
  runner: '{{ $registry.prefix.owner }}runner{{ $all.sep.image }}'
image:
  source: 'docker.io{{ $all.sep.registry }}library{{ $all.sep.registry }}{{ $registry.id }}'
  local: '{{ $registry.prefix.local }}{{ $registry.id }}'
port: 5000
tag: '2'
target: '{{ $registry.ip }}{{ $all.sep.target }}{{ $registry.port }}'
volume: '{{ $registry.id }}'

...
