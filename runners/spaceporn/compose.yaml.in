services:
  ${RUNNER_ID}:
    build:
      context: .
      args:
        - OS_IMG=${OS_IMG}
        - ZIG_ITERATOR_IMG=${ZIG_ITERATOR_IMG}
    container_name: ${SPACEPORN_RUNNER_ID}
    image: ${SPACEPORN_RUNNER_IMG}
    stdin_open: true
    tty: true
