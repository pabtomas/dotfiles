#! /bin/sh

host "${COLLECTOR_ID}"
host "${CONTROLLER_ID}"
host "${EDITOR_ID}"
host "${JUMPER_ID}"
host "${MAN_ID}"
host "${PROXY_ID}"
host "${SAFEDEPOSIT_ID}"
host "${SCHOLAR_ID}"

explorer_host "${SHELL_ID}"
explorer_host "${ZIG_ID}"
